defmodule Wik.Repo.Migrations.MigrateResources15 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:memberships) do
      add :primary_block_id,
          references(:blocks,
            column: :id,
            name: "memberships_primary_block_id_fkey",
            type: :uuid,
            prefix: "public"
          )
    end
  end

  def down do
    drop constraint(:memberships, "memberships_primary_block_id_fkey")

    alter table(:memberships) do
      remove :primary_block_id
    end
  end
end
