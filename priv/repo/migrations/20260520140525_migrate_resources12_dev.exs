defmodule Wik.Repo.Migrations.MigrateResources12 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:taggings) do
      modify :taggable_id,
             references(:group_user_relations,
               column: :id,
               name: "taggings_taggable_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    create index(:taggings, [:group_id, :taggable_id])
  end

  def down do
    drop_if_exists index(:taggings, [:group_id, :taggable_id])

    drop constraint(:taggings, "taggings_taggable_id_fkey")

    alter table(:taggings) do
      modify :taggable_id, :uuid
    end
  end
end
